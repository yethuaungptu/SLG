<!DOCTYPE html>
<html lang="en" data-theme="mytheme">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sustainable Living Guide</title>
    <link href="/stylesheets/style.css" rel="stylesheet" />
    <link rel="stylesheet" href="/stylesheets/aos.css" />
  </head>
  <body class="bg-white text-gray-800 aos-all">
    <% include ./partial/navbar.ejs %>

    <!-- Hero Section with Full Glass Background -->
    <section
      class="relative bg-green-900 bg-cover bg-center py-20 px-4 aos-item"
      data-id="1"
      data-aos="fade-up"
      style="background-image: url('/images/slider-1.jpg')"
    >
      <div class="absolute inset-0 bg-black/50"></div>
      <!-- Optional dark overlay -->

      <div class="relative max-w-7xl mx-auto">
        <div
          class="backdrop-blur-lg bg-white/10 rounded-xl shadow-2xl p-10 flex flex-col md:flex-row items-center gap-8 text-white"
        >
          <!-- Text Content -->
          <div class="flex-1">
            <h1 class="text-4xl md:text-5xl font-bold leading-tight mb-6">
              Choose Eco friendly, Live Sustainably
            </h1>
            <p class="mb-6 text-lg">
              Discover practical tips, inspiring stories, and actionable
              challenges to help you reduce your environmental impact and make
              sustainable living a part of your everyday life.
            </p>
            <div class="flex gap-4">
              <a href="/challenges" class="btn btn-success">Start Challenge</a>
              <a href="/tips" class="btn btn-outline text-white border-white">
                Start Tip
              </a>
            </div>
          </div>

          <!-- Image -->
          <div class="flex-1 hidden md:block">
            <img
              src="/images/index-1.jpg"
              alt="Plant in soil"
              class="rounded-lg w-full shadow-lg"
            />
          </div>
        </div>
      </div>
    </section>
    <section data-id="2" data-aos="fade-up" class="py-20 px-6 aos-item">
      <div class="max-w-6xl mx-auto text-center mb-10">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-800">
          Featured Tips
        </h2>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-6xl mx-auto">
        <% if (featuredTips.length === 0) { %>
        <div class="text-center col-span-3 mt-3">
          <p class="text-lg font-semibold">No featured tips available.</p>
        </div>
        <%} %> <% for(var i = 0; i < featuredTips.length;i++){%>
        <div class="relative bg-transparent">
          <% if (featuredTips[i].image) { %>
          <img
            src="<%= featuredTips[i].image %>"
            alt="Garden Design"
            class="w-full h-128 object-cover rounded-xl shadow-lg"
          />
          <%}else{%>
          <img
            src="/images/slider-1.jpg"
            alt="Garden Design"
            class="w-full h-128 object-cover rounded-xl shadow-lg"
          />
          <%}%>
          <div
            class="absolute bottom-[20px] left-1/2 transform -translate-x-1/2 w-11/12 bg-white/40 backdrop-blur-md rounded-xl p-5 shadow-lg"
          >
            <h3 class="text-xl font-bold text-green-700 mb-1 text-center">
              <%= featuredTips[i].title %>
            </h3>
            <p class="text-gray-800 text-sm text-center line-clamp-3">
              <%= featuredTips[i].description %>
            </p>
            <div class="card-actions mt-2">
              <a
                class="btn btn-primary btn-sm mx-auto"
                href="/tips/<%= featuredTips[i]._id %>"
                >More Details</a
              >
            </div>
          </div>
        </div>
        <%}%>
      </div>
    </section>
    <section data-id="3" data-aos="fade-up" class="py-20 px-6 aos-item">
      <div class="max-w-6xl mx-auto text-center mb-10">
        <h2 class="text-3xl md:text-4xl font-bold text-green-800">
          Featured Challenge
        </h2>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-6xl mx-auto">
        <% if (featuredChallenges.length === 0) { %>
        <div class="text-center col-span-3 mt-3">
          <p class="text-lg font-semibold">No featured tips available.</p>
        </div>
        <%} %> <% for(var i = 0; i < featuredChallenges.length;i++){%>
        <div class="relative bg-transparent">
          <% if (featuredChallenges[i].image) { %>
          <img
            src="<%= featuredChallenges[i].image %>"
            alt="Garden Design"
            class="w-full h-128 object-cover rounded-xl shadow-lg"
          />
          <%}else{%>
          <img
            src="/images/slider-1.jpg"
            alt="Garden Design"
            class="w-full h-128 object-cover rounded-xl shadow-lg"
          />
          <%}%>
          <div
            class="absolute bottom-[20px] left-1/2 transform -translate-x-1/2 w-11/12 bg-white/40 backdrop-blur-md rounded-xl p-5 shadow-lg"
          >
            <h3 class="text-xl font-bold text-green-700 mb-1 text-center">
              <%= featuredChallenges[i].name %>
            </h3>
            <p class="text-gray-800 text-sm text-center line-clamp-3">
              <%= featuredChallenges[i].description %>
            </p>
            <div class="card-actions mt-2">
              <a
                class="btn btn-primary btn-sm mx-auto"
                href="/challenges/<%= featuredChallenges[i]._id %>"
                >More Details</a
              >
            </div>
          </div>
        </div>
        <%}%>
      </div>
    </section>
    <!-- Video Section -->
    <section data-id="4" data-aos="fade-up" class="py-20 px-6 aos-item">
      <div class="max-w-6xl mx-auto text-center mb-10">
        <h2 class="text-3xl md:text-4xl font-bold text-green-800">
          Explore Sustainable Living
        </h2>
        <p class="mt-3 text-gray-600">
          Watch our quick video to see how small changes can make a big impact.
        </p>
      </div>

      <div class="max-w-4xl mx-auto rounded-xl overflow-hidden shadow-2xl">
        <video
          class="w-full rounded-xl"
          controls
          preload="none"
          poster="/images/video_poster.png"
        >
          <source src="/video/AI_Video.mp4" type="video/mp4" />
          Your browser does not support the video tag.
        </video>
      </div>
    </section>
    <% include ./partial/footer.ejs %>
  </body>
  <script src="/javascripts/aos.js"></script>
  <script>
    AOS.init({
      mirror: true,
    });
  </script>
</html>

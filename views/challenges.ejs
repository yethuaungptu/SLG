<!DOCTYPE html>
<html data-theme="mytheme">
  <head>
    <title>Sustainable Living Guide</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link rel="stylesheet" href="/stylesheets/aos.css" />
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  </head>
  <body>
    <% include ./partial/navbar.ejs %>
    <div data-id="1" class="aos-item my-5" data-aos="fade-up">
      <div class="text-4xl font-bold text-center"><%= title %></div>
      <div class="row">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-6xl mx-auto">
          <% if (challenges.length === 0) { %>
          <div class="text-center col-span-3 mt-3">
            <p class="text-lg font-semibold">No challenges available.</p>
          </div>
          <%} %> <% for(var i = 0; i < challenges.length;i++){%>
          <div class="relative bg-transparent">
            <% if (challenges[i].image) { %>
            <img
              src="<%= challenges[i].image %>"
              alt="Garden Design"
              class="w-full h-128 object-cover rounded-xl shadow-lg"
            />
            <%}else{%>
            <img
              src="/images/slider-1.jpg"
              alt="Garden Design"
              class="w-full h-128 object-cover rounded-xl shadow-lg"
            />
            <%}%>
            <div
              class="absolute bottom-[20px] left-1/2 transform -translate-x-1/2 w-11/12 bg-white/40 backdrop-blur-md rounded-xl p-5 shadow-lg"
            >
              <h3 class="text-xl font-bold text-green-700 mb-1 text-center">
                <%= challenges[i].name %>
              </h3>
              <p class="text-gray-800 text-sm text-center line-clamp-3">
                <%= challenges[i].description %>
              </p>
              <div class="card-actions mt-2">
                <a
                  class="btn btn-primary btn-sm mx-auto"
                  href="/challenges/<%= challenges[i]._id %>"
                  >More Details</a
                >
              </div>
            </div>
          </div>
          <%}%>
        </div>
      </div>
    </div>
    <% include ./partial/footer.ejs %>
  </body>
  <script src="/javascripts/aos.js"></script>
  <script>
    AOS.init({
      mirror: true,
    });
  </script>
</html>
